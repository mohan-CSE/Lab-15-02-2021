#  Fills all elements of a list with a given value.
#  @param data the list whose elements are to be changed
#  @param value the value placed in each element of the list
#
def fill(data, value) :
    for i in range(len(data)):
       data[i]=value
